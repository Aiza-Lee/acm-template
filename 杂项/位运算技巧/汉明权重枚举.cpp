/**
 * 获得 0-n 按照汉明权重递增的排列
 */
void foo() {
	int n = 114514;
	// 
	for (int i = 0; (1 << i) - 1 <= n; i++) {
		for (
			int x = (1 << i) - 1, t; x <= n; 
			t = x + (x & -x), 
			x = x ? (t | ((((t & -t) / (x & -x)) >> 1) - 1)) : (n + 1)) {
			// x 即为对应的二进制编码
		}
	}
}