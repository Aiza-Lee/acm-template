\textbf{原根判定定理} \\
对于整数 $m>3$ 且 $g\perp m$，$g$ 是模 $m$ 的原根的充分必要条件是，对于 $\varphi(m)$ 的每个素因子 $p$，都有：

\[g^{\frac{\varphi(m)}{p}}\not\equiv 1\pmod{m}.\]

\textbf{原根存在定理} \\
模 $m$ 有原根的充分必要条件是 $m=1,2,4,p^k,2p^k$，其中 $p$ 是奇素数，$k$ 是正整数。